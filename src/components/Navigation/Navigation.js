import React from "react";

const Navigation = () => {
  return (
    <nav
      style={{
        display: "flex",
        justifyContent: "flex-end",
        paddingRight: "2rem",
      }}
    >
      <p>Sign Out</p>
    </nav>
  );
};

export default Navigation;
